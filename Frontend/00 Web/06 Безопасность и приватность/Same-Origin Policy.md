# Same-Origin Policy

> [!todo] Заметка к созданию
> SOP: определение origin (scheme + host + port), ограничения, исключения.
> CORS — механизм управления чтением cross-origin ответов, а не «запрет запросов».
