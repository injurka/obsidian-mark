Операторы эквивалентности (`==`, `!=`, `===`, `!==`) сравнивают значения и возвращают логическое значение.

```ts
let a = 5;
let b = "5";

console.log(a == b); // true
console.log(a != b); // false
console.log(a === b); // false
console.log(a !== b); // true
```