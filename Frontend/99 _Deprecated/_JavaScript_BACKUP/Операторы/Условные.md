Условный оператор (`? :`) является тернарным оператором и возвращает одно значение, если условие истинно, и другое значение, если условие ложно.

```ts
let age = 18;
let message = age >= 18 ? "Adult" : "Minor";

console.log(message); // "Adult"
```