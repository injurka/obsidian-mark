Напиши содержимое последовательно для файлов в директории: @05-runtime-dom/ и вставь в них текст (текст выписывать в терминал не нужно, только заполни файлы).

Действуй как Core-контрибьютор фреймворка Vue.js и Senior Software Engineer, глубоко понимающий архитектуру современных UI-фреймворков. 

Моя цель — заполнить базу знаний "Vue Internals", чтобы мыслить на уровне создателей фреймворка. Лаконичное, связное повествование. Раскрой суть концепции как хорошую инженерную историю.

ТРЕБОВАНИЯ К СТИЛЮ И ПОДАЧЕ:
1. Повествовательно, но лаконично: пиши как инженер для инженера. Без "воды", долгих вступлений, маркетинга и базовых туториалов "как использовать эту фичу во Vue". Сразу к архитектуре и внутреннему устройству.
2. Фокус на "Under the Hood": объясняй структуры данных, алгоритмы, паттерны проектирования и причины принятия архитектурных решений (trade-offs).
3. Придерживайся актуальных реалий Vue (версии 3.4+ / 3.5+, Vapor Mode, если применимо).

СТРУКТУРА ФАЙЛА:
1. **Концепция и Архитектура (Mental Model):** Кратко объясни суть механизма. Зачем он нужен ядру? Какую проблему решает?
2. **Визуализация (Mermaid):** Обязательно интегрируй Mermaid-диаграмму (flowchart, state diagram, class diagram или sequence diagram), которая наглядно показывает процесс (например, флоу рендеринга, движение данных по графу реактивности, стейт-машину парсера).
3. **Ссылки на исходный код (Source Code References):** Укажи точные или примерные пути к файлам в репозитории `vuejs/core` (например, `packages/reactivity/src/effect.ts`).
4. **Разбор реализации (Code Deep Dive):** Приведи упрощенные выдержки реального исходного кода фреймворка. Прокомментируй ключевые строки. Покажи, как это работает на уровне TypeScript.
5. **Оптимизации и Edge Cases (Подводные камни):** Почему код написан именно так? (Например: использование побитовых операций (bitwise) для скорости, двусвязные списки вместо массивов для GC-оптимизации, грязные хаки для обхода ограничений браузеров/JS-движков).

ФОРМАТ ВЫВОДА:
Выдай только чистый Markdown-код, готовый к копированию в файл. Не пиши вводных и заключительных фраз вроде "Вот ваш файл" или "Надеюсь, это поможет". Используй заголовки, списки, блоки кода и цитаты для форматирования.
